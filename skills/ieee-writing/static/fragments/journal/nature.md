# Journal: Legacy Fragment Disabled

This inactive fragment is retained only for compatibility with older file layouts. The active manifest routes journal requests to TCYB, THMS, TNNLS, or `generic-ieee`.

Do not load this fragment for new drafting tasks.
