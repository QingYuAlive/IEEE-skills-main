# Disabled Legacy Template Index

This file is intentionally inactive. The active template source is `static/fragments/backend/python.md`.

When adapting a prior plotting template, implement the result as a fresh Python script that includes:

- `ieee_style_setup()`
- exact IEEE width selection
- LaTeX math rendering
- explicit input validation
- PDF and PNG export

Do not load or adapt image/PDF assets.
