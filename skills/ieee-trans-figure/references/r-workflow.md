# Disabled Legacy Backend Workflow

This file remains only to preserve the cloned directory structure. The active `ieee-trans-figure` skill generates Python code only.

If a user supplies a script from another plotting environment, translate the requested figure into Python with Matplotlib/Seaborn after the user provides exact data arrays, tables, or mathematical functions.

Do not generate code for any non-Python plotting backend from this skill.
